# 运行说明和运行时间估算

## 🚀 运行方式

### 是的，运行 `main.py`

```bash
python main.py
```

---

## ⏱️ 运行时间估算

### 当前网格设置（与MATLAB一致）

**网格大小**:
- `nx = 60`（生产率网格）
- `nb = 80`（债务网格）
- `nk = 100`（资本网格）
- **总状态点数** = 60 × 80 × 100 = **480,000**

**迭代参数**:
- `max_iter = 4000`（VFI最大迭代次数）
- `maxiter_dist = 10000`（分布最大迭代次数）
- `tol_vfi = 1e-9`（VFI容差，非常严格）
- `tol_dist = 1e-6`（分布容差）

---

## 📊 运行时间估算（基于当前网格60×80×100）

### 单次稳态计算（do_calib = 0）

**计算步骤**:

1. **Step 1: 价格计算**
   - 时间: **< 1秒**
   - 复杂度: O(1)

2. **Step 2: 价值函数迭代（VFI）**
   - 时间: **15-45分钟**
   - 复杂度: O(nk × nb × nx × max_iter) = 480,000 × 4000 = 1,920,000,000次操作
   - 影响因素:
     - 收敛速度（取决于初始猜测和参数）
     - Howard加速（n_howard=50）
     - 容差设置（tol_vfi=1e-9，非常严格）

3. **Step 3: 分布计算**
   - 时间: **10-30分钟**
   - 复杂度: O(nk × nb × nx × maxiter_dist) = 480,000 × 10000 = 4,800,000,000次操作
   - 影响因素:
     - 收敛速度（取决于分布初始值）
     - 容差设置（tol_dist=1e-6）

4. **Step 4: 加总变量计算**
   - 时间: **1-3分钟**
   - 复杂度: O(nk × nb × nx) = 480,000次操作

5. **Step 5: 模型矩计算**
   - 时间: **1-3分钟**
   - 复杂度: O(nk × nb × nx) = 480,000次操作

**总计**: **约 30-80分钟**（取决于收敛速度）

---

### 稳态 + 转移动态（do_calib = 1）

**稳态计算**: 30-80分钟（同上）

**转移动态计算**（T=50期）:
- 每期计算: **1-3分钟**
- 50期总计: **50-150分钟**（约1-2.5小时）

**总计**: **约 1.5-4小时**

---

### 校准稳态（do_calib = 2）

**单次目标函数评估**:
- 稳态计算: 30-80分钟
- 目标函数计算: 1-2分钟
- **总计**: **约 30-80分钟**

**完整校准**（假设100次迭代）:
- 单次评估: 30-80分钟
- 100次迭代: **50-130小时**（约2-5天）
- 加上优化器开销: **60-150小时**（约2.5-6天）

**快速测试**（5次迭代）:
- 5次迭代: **2.5-7小时**

---

## 📈 与MATLAB对比

| 项目 | MATLAB | Python（当前） | 说明 |
|------|--------|---------------|------|
| 网格大小 | 60×80×100 | 60×80×100 | ✅ 一致 |
| 单次稳态 | 30-60分钟 | 30-80分钟 | ✅ 接近 |
| 转移动态 | 25-75分钟 | 50-150分钟 | ⚠️ Python稍慢 |

**注意**: Python版本可能稍慢，因为：
- 没有使用MEX文件（Fortran编译）
- 纯Python实现，依赖NumPy向量化
- 但结果应该一致

---

## 🎯 实际运行建议

### 1. 首次运行（do_calib = 0）

```bash
python main.py
```

**预计时间**: **30-80分钟**

**建议**:
- ✅ 确保有足够的时间（至少1-2小时）
- ✅ 可以在后台运行
- ✅ 查看日志文件 `steady_state_iterations.log` 了解进度

---

### 2. 监控运行进度

**查看日志文件**:
```bash
# Windows PowerShell
Get-Content steady_state_iterations.log -Wait

# Linux/Mac
tail -f steady_state_iterations.log
```

**日志会显示**:
- 每个步骤的开始和结束时间
- 迭代进度
- 收敛信息
- 警告和错误

---

### 3. 如果运行时间过长

**可以尝试**:

1. **检查收敛速度**
   - 查看日志中的迭代次数
   - 如果迭代次数接近最大值，可能需要调整初始猜测

2. **临时放宽容差**（仅用于测试）
   ```python
   # 在set_parameters.py中临时修改
   par['tol_vfi'] = 1e-6  # 从1e-9改为1e-6
   par['tol_dist'] = 1e-4  # 从1e-6改为1e-4
   ```
   **注意**: 这会影响精度，仅用于快速测试

3. **使用较小的网格**（仅用于测试）
   ```python
   # 在set_parameters.py中临时修改
   par['nx'] = 40  # 从60改为40
   par['nb'] = 50  # 从80改为50
   par['nk'] = 60  # 从100改为60
   ```
   **注意**: 这会影响精度，仅用于快速测试

---

## ⚠️ 注意事项

### 1. 内存使用

**网格大小 60×80×100**:
- 主要数组大小: 480,000个浮点数
- 每个数组约: 480,000 × 8字节 = 3.8 MB
- 总内存使用: **约500MB-2GB**（取决于中间变量）

**建议**: 确保有至少4GB可用内存

---

### 2. 磁盘空间

**输出文件**:
- `steady_state_results.pkl`: 约50-200MB
- `steady_state_iterations.log`: 约1-10MB

**建议**: 确保有至少500MB可用磁盘空间

---

### 3. CPU使用

**单核使用**:
- Python版本使用单线程（已移除并行计算）
- NumPy的BLAS库会自动利用多核（如果使用支持并行的版本）

**建议**: 
- 使用支持OpenBLAS或MKL的NumPy版本
- 会自动利用多核CPU

---

## 📝 运行检查清单

运行前检查：

- [ ] 已安装所有依赖（`pip install -r requirements.txt`）
- [ ] 有足够的磁盘空间（至少500MB）
- [ ] 有足够的内存（至少4GB）
- [ ] 有足够的时间（至少1-2小时）
- [ ] 已设置 `do_calib = 0`（如果只想计算稳态）

运行中检查：

- [ ] 查看日志文件了解进度
- [ ] 检查是否有错误信息
- [ ] 监控内存和CPU使用情况

运行后检查：

- [ ] 检查 `steady_state_results.pkl` 是否生成
- [ ] 检查 `flag_ss` 是否为非负值（表示成功）
- [ ] 查看日志文件中的最终结果

---

## 🎯 总结

**运行方式**: 
```bash
python main.py
```

**预计时间**:
- **单次稳态计算**: **30-80分钟**
- **稳态 + 转移动态**: **1.5-4小时**
- **完整校准**: **2-6天**（100次迭代）

**建议**: 
- ✅ 首次运行选择 `do_calib = 0`（仅稳态）
- ✅ 确保有足够的时间和资源
- ✅ 查看日志文件了解进度
- ✅ 如果时间过长，可以临时放宽容差或减小网格（仅用于测试）

---

生成时间: 2026-01-05









