# 重新校准参数指南

## 目标

由于使用MATLAB的网格（60×80×100）和容差（1e-9）计算太慢，我们需要：
- ✅ 使用较小的网格（校准模式：30×40×50）
- ✅ 重新校准参数以适应这个网格
- ✅ 找到合理的参数值

---

## 当前设置

### 校准模式网格（do_calib=2）
- nx = 30
- nb = 40
- nk = 50
- 总状态点数 = 60,000

### 容差设置（校准模式）
- tol_vfi = 1e-4
- tol_dist = 1e-3
- tol_bhat = 1e-5

### 优化器设置
- 算法：L-BFGS-B
- 最大迭代次数：100
- 容差：ftol=1e-4, gtol=1e-4

---

## 运行校准

### 步骤1：备份参数文件

校准会更新`inputs/estim_params.txt`，建议先备份：

```bash
# 备份文件（程序会自动备份）
# 备份文件会保存为: inputs/estim_params_backup.txt
```

### 步骤2：运行完整校准

```bash
python run_calibration.py
```

**预计时间**：8-23小时（取决于计算机性能）

**过程**：
- 每次迭代会打印当前参数值
- 会显示市场出清检查（K_corp, LHS, aux）
- 会显示目标函数值

### 步骤3：监控进度

**关键指标**：
1. **目标函数值**：应该逐渐下降
2. **参数值变化**：应该稳定收敛
3. **市场出清条件**：K_corp和LHS应该变为正值

**如果目标函数不再下降**：
- 可以按Ctrl+C中断
- 使用当前最佳参数值

---

## 快速测试（可选）

如果想先快速验证，可以运行快速测试：

```bash
python quick_test_calibration.py
```

**预计时间**：30-90分钟（5次迭代）

**用途**：
- 验证校准过程是否正常
- 查看参数变化趋势
- 如果改善，再运行完整校准

---

## 校准后的验证

### 步骤1：使用校准后的参数

校准完成后，参数会自动保存到`inputs/estim_params.txt`

### 步骤2：在正常模式网格上运行

```python
# 在main.py中设置
par['do_calib'] = 0  # 正常模式，使用50×60×70网格
```

然后运行：
```bash
python main.py
```

### 步骤3：检查结果

**预期结果**：
- ✅ output_small应该接近合理值（可能0.05-0.1）
- ✅ LHS应该为正值
- ✅ K_corp应该为正值
- ✅ 所有加总变量应该为正值

---

## 预期参数变化

### 当前参数值（MATLAB校准的）
```
mass         = 0.045
fixcost1     = 0.1652544033
fixcost2     = 0.0047081982
theta        = 0.9094588315
psi          = 0.0039413875
k_alpha      = 0.4458454251
x0           = 1.0743375933
epsx         = 0.0983399600
rhox         = 0.9572858171
zeta         = 23.4199308730
```

### 校准后（预期）
- 参数值可能会变化
- 但应该在经济合理范围内
- 应该满足市场出清条件

---

## 注意事项

### 1. 时间较长
- 完整校准需要8-23小时
- 建议在后台运行或使用服务器
- 可以随时按Ctrl+C中断

### 2. 参数文件更新
- 校准过程中会更新`inputs/estim_params.txt`
- 程序会自动备份原始文件
- 备份文件：`inputs/estim_params_backup.txt`

### 3. 市场出清惩罚项
- Python添加了市场出清惩罚项
- 这应该能引导优化器找到合理的参数值
- 如果K_corp<0或LHS<0，会有大惩罚

### 4. 收敛检查
- 如果目标函数不再下降，可以提前停止
- 如果参数值稳定，可以提前停止
- 按Ctrl+C可以安全中断

---

## 故障排除

### 问题1：校准失败
- **检查**：参数是否在边界内
- **检查**：目标函数是否正常计算
- **解决**：调整初始猜测值或边界

### 问题2：结果不合理
- **检查**：市场出清条件是否满足
- **检查**：模型矩是否接近数据矩
- **解决**：增加迭代次数或调整权重

### 问题3：速度太慢
- **检查**：网格大小是否合理（30×40×50）
- **检查**：容差是否合理（1e-4, 1e-3）
- **解决**：可以进一步缩小网格或放宽容差

---

## 总结

**策略**：
1. ✅ 使用较小的网格（30×40×50）进行校准
2. ✅ 运行完整校准（100次迭代）
3. ✅ 找到适合的参数值
4. ✅ 使用这些参数值在正常模式网格（50×60×70）上运行

**时间**：
- 快速测试：30-90分钟
- 完整校准：8-23小时

**预期**：
- 校准后应该得到合理的结果
- output_small应该接近合理值
- LHS和K_corp应该变为正值













