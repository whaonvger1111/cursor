# 稳态计算问题诊断报告

## 关键问题总结

### 1. 市场出清方程左端 (LHS) 为负值

**值**: LHS = -66.955868

**分解**:
```
C_agg (总消费)           =   0.108363
output_small (小企业产出)  =  28.473263
cost_adj (资本调整成本)    = -35.562172  ← 负值
entry_cost (进入成本)     =   1.021903
liq (清算)               =   4.050698

LHS = 0.108363 - 28.473263 + (-35.562172) + 1.021903 - 4.050698
LHS = -66.955868
```

**问题**：
- LHS < 0 导致 K_corp = LHS / aux = -66.955868 / 0.072074 = **-928.981548**（负值）
- 这意味着市场出清条件不满足

**原因分析**：
1. **output_small过大** (28.47) > **C_agg过小** (0.108)
2. **cost_adj为负值** (-35.56)，进一步降低了LHS
3. **总需求 < 总供给**：消费+调整成本+进入成本 < 产出+清算

---

### 2. 企业部门资本 K_corp 为负值

**值**: K_corp = -928.981548

**计算**:
```
K_corp = LHS / aux
K_corp = -66.955868 / 0.072074
K_corp = -928.981548
```

**影响**：
- 导致 L_corp = K_corp / KL_ratio = -205.897582（负值）
- 导致 Y_corp = Fun.prod_corp(KL_ratio, L_corp) = -80.890591（负值）
- 导致所有相关的加总变量为负值

---

### 3. 资本调整成本 cost_adj 为负值

**值**: cost_adj = -35.562172

**分解**:
```
capadj[0] (向上调整)     =  69.187434
capadj[1] (向下调整)     = 130.943957  ← 远大于向上调整
capadj[2] (进入者购买)   =   1.021903
capadj[3] (退出者出售)   =   4.521792
```

**计算方式**:
```
cost_adj = sum(adjcost(kp, k, theta, delta) * mu_active)

adjcost定义：
  - 如果 kp >= (1-delta)*k: adjcost = kp - (1-delta)*k (正值)
  - 如果 kp < (1-delta)*k:  adjcost = theta * (kp - (1-delta)*k) (负值)
```

**问题**：
- 向下调整 (130.94) >> 向上调整 (69.19)
- 净调整成本为负值，说明企业**净收缩资本**
- 这在经济上可能合理，但导致LHS为负值

---

### 4. entry_vec 中存在负值

**问题**:
```
entry_vec中存在负值！
最小值 = -5.674426e-08
负值数量 = 91
```

**可能原因**：
1. **数值误差**：浮点数计算误差
2. **插值问题**：`find_loc` 函数可能返回了不正确的索引
3. **分布计算问题**：`phi_dist` 或 `pol_entry` 可能有问题

**影响**：
- entry_vec应该是非负的（进入者分布）
- 负值可能导致分布计算错误

---

### 5. 总消费 C_agg 过小

**值**: C_agg = 0.108363

**计算方式**:
```python
C_agg = Fun.C_foc_labor(wage, par)
C_agg = (wage / zeta) ** (1 / sigma)
C_agg = (0.275008 / 23.419931) ** (1 / 2.0)
C_agg = 0.108363
```

**问题**：
- C_agg = 0.108 << output_small = 28.473
- 消费过小，无法支撑产出

**可能原因**：
1. **工资wage过小** (0.275008)
2. **zeta过大** (23.419931)，导致消费过小
3. **sigma = 2.0**，CRRA参数可能不合适

---

## 根本原因分析

### 问题1：参数值不合理

**当前参数值**（来自快速测试，几乎等于初始值）：
```
mass         = 0.045
fixcost1     = 0.1652544033
fixcost2     = 0.0047081982
theta        = 0.9094588315
psi          = 0.0039413875
k_alpha      = 0.4458454251
x0           = 1.0743375933
epsx         = 0.0983399600
rhox         = 0.9572858171
zeta         = 23.4199308730  ← 可能过大
```

**问题**：
- 这些参数值可能**不适合当前模型设定**
- 快速测试只运行了5次迭代，参数几乎未变
- 需要**完整校准**来找到合理的参数值

---

### 问题2：模型矩严重偏离

**关键矩的偏离**：
- `empshare_small = 0.0000`（应为0.4895）
- `ave_work = -113.6842`（应为0.3300）
- 多个矩为0

**说明**：
- 当前参数组合无法产生合理的模型结果
- 需要调整参数值

---

## 解决方案

### 方案1：运行完整校准（推荐）

**步骤**：
1. 运行完整校准（至少50-100次迭代）
2. 让优化器找到合理的参数值
3. 验证校准后的参数值是否满足市场出清条件

**命令**：
```bash
python run_calibration.py
```

---

### 方案2：调整初始参数值

**如果完整校准仍然失败**：
1. 检查MATLAB的校准结果
2. 使用MATLAB的校准参数值作为初始值
3. 重新运行校准

---

### 方案3：修复 entry_vec 负值问题

**检查点**：
1. `fun_distrib1.py` 中 `entry_vec` 的计算
2. `find_loc` 函数的返回值
3. `phi_dist` 和 `pol_entry` 的值

**可能的修复**：
```python
# 在计算 entry_vec 后，确保非负
entry_vec = np.maximum(entry_vec, 0)
```

---

### 方案4：调整市场出清惩罚项

**当前惩罚项**（`fun_obj.py`）：
```python
if K_corp < 0:
    market_clearing_penalty += 10000 * abs(K_corp)
if LHS < 0:
    market_clearing_penalty += 10000 * abs(LHS)
```

**建议**：
- 惩罚项已经存在，应该能引导优化器
- 但需要更多迭代才能生效

---

## 建议的下一步行动

### 优先级1：运行完整校准

1. **修改 `run_calibration.py`**：
   - 增加迭代次数到至少50-100次
   - 检查收敛条件

2. **运行校准**：
   ```bash
   python run_calibration.py
   ```

3. **监控过程**：
   - 观察目标函数值是否下降
   - 观察参数值是否变化
   - 观察市场出清条件是否改善

---

### 优先级2：修复 entry_vec 负值

1. **检查 `fun_distrib1.py`**：
   - 验证 `entry_vec` 的计算逻辑
   - 添加非负性检查

2. **检查 `find_loc` 函数**：
   - 确保返回值正确
   - 检查边界情况

---

### 优先级3：诊断消费过小问题

1. **检查工资计算**：
   - 验证 `fun_prices.py` 中的工资计算
   - 检查 `zeta` 和 `sigma` 的值

2. **对比MATLAB**：
   - 查看MATLAB中相同参数值下的消费值
   - 确认计算逻辑一致

---

## 总结

### 当前状态

- ❌ **市场出清条件不满足**：LHS < 0, K_corp < 0
- ❌ **参数值不合理**：快速测试的参数值几乎未变
- ⚠️ **entry_vec有负值**：可能是数值误差
- ⚠️ **消费过小**：C_agg = 0.108 << output_small = 28.473

### 根本原因

**参数值未经过充分校准**：
- 快速测试只运行了5次迭代
- 参数值几乎等于初始值
- 这些初始值可能不适合当前模型设定

### 解决方案

**运行完整校准**：
- 增加迭代次数
- 让优化器找到合理的参数值
- 验证市场出清条件是否满足













