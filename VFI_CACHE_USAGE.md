# 无约束企业VFI结果缓存使用说明

## ✅ 功能说明

实现了无约束企业VFI结果的缓存机制，可以将计算结果存储到文件，下次计算约束企业VFI时直接调用，避免重复计算。

---

## 🚀 使用方法

### 1. 自动缓存（默认启用）

缓存功能默认启用，无需额外配置。程序会自动：
- 在计算无约束企业VFI之前检查缓存
- 如果缓存存在且参数匹配，直接加载结果
- 如果缓存不存在，进行计算并保存结果

### 2. 禁用缓存

如果需要禁用缓存（例如参数发生变化），可以在参数字典中设置：

```python
par['use_vfi_cache'] = False  # 禁用缓存
```

### 3. 自定义缓存目录

默认缓存目录为 `cache/`，可以自定义：

```python
par['cache_dir'] = 'my_cache'  # 自定义缓存目录
```

---

## 📁 缓存文件

### 缓存文件位置

缓存文件保存在 `cache/` 目录下（或自定义目录），文件名格式：
```
unconstrained_vfi_{cache_key}.pkl
```

其中 `cache_key` 是基于价格和关键参数的MD5哈希值。

### 缓存文件内容

缓存文件包含以下数据：
- `V1`: 无约束企业的价值函数 (nk, nx)
- `x_tilde`: 生产率截断值索引 (nk,)
- `x_tilde_val`: 生产率截断值 (nk,)
- `pol_kp_unc`: 无约束投资政策 (nk, nx)
- `B_hat`: B_hat固定点 (nk, nx)
- `pol_bp_unc`: 无约束债务政策
- `b_grid`: 债务网格 (nk, nb)
- `kp_bar`: 下一期资本上界 (nk, nb, nx)
- `val_unc`: 无约束企业退出后的价值 (nk, nb, nx)
- `val0_unc`: 无约束企业退出前的价值 (nk, nb, nx)
- `profit_mat`: 利润矩阵 (nk, nx)
- `b_tilde`: b_tilde值 (nk,)

---

## 🔑 缓存键生成

缓存键基于以下参数生成：
- `q`: 债券价格
- `wage`: 工资
- `nx`: 生产率网格大小
- `nb`: 债务网格大小
- `nk`: 资本网格大小
- `theta`: 参数
- `delta`: 折旧率
- `psi`: 参数
- `lambda`: 参数

**注意**: 如果这些参数中的任何一个发生变化，缓存键也会改变，程序会重新计算。

---

## 💡 使用场景

### 场景1: 多次运行稳态计算

如果多次运行稳态计算（例如校准过程），无约束企业VFI的结果通常不变，使用缓存可以显著节省时间。

### 场景2: 调试约束企业VFI

如果只需要调试约束企业VFI部分，可以：
1. 第一次运行完整计算（生成缓存）
2. 后续运行直接使用缓存，跳过无约束企业VFI计算

### 场景3: 参数敏感性分析

如果只改变约束企业相关的参数，无约束企业VFI的结果可以复用。

---

## 🛠️ 管理缓存

### 清除缓存

可以使用Python代码清除缓存：

```python
from cache_unconstrained_vfi import clear_unconstrained_vfi_cache

# 清除默认缓存目录
clear_unconstrained_vfi_cache()

# 清除自定义缓存目录
clear_unconstrained_vfi_cache('my_cache')
```

### 手动删除缓存文件

也可以直接删除 `cache/` 目录下的 `unconstrained_vfi_*.pkl` 文件。

---

## ⚠️ 注意事项

1. **参数匹配**: 缓存键基于关键参数生成，如果参数发生变化，缓存会自动失效
2. **文件大小**: 缓存文件可能较大（取决于网格大小），注意磁盘空间
3. **版本兼容**: 如果代码结构发生变化，可能需要清除旧缓存
4. **并发访问**: 多个进程同时运行时，缓存文件可能冲突（建议使用不同的缓存目录）

---

## 📊 性能提升

### 预期时间节省

- **无约束企业VFI**: 约10-15秒（取决于迭代次数）
- **B_hat固定点**: 约5-10秒（取决于迭代次数）
- **总计**: 每次运行可节省约15-25秒

### 实际效果

- 第一次运行: 正常计算时间 + 保存缓存时间（可忽略）
- 后续运行: 加载缓存时间（<1秒）+ 约束企业VFI计算时间

---

## 🔍 调试信息

程序会输出缓存相关的信息：

```
从缓存加载无约束企业VFI结果 (cache_key: 9ac4b0a2...)
```

或

```
缓存未找到，开始计算无约束企业VFI (cache_key: 9ac4b0a2...)
无约束企业VFI结果已保存到缓存: cache/unconstrained_vfi_9ac4b0a2e71cae460c2160dbbf224448.pkl
```

---

## 📝 示例代码

```python
# 启用缓存（默认）
par['use_vfi_cache'] = True
par['cache_dir'] = 'cache'

# 运行计算
sol, b_grid, phi_dist, conv_flag = fun_vfi1(prices, par)

# 禁用缓存
par['use_vfi_cache'] = False
sol, b_grid, phi_dist, conv_flag = fun_vfi1(prices, par)
```

---

生成时间: 2026-01-05









