# 重新校准参数策略

## 问题

使用MATLAB的网格（60×80×100）和容差（1e-9）计算太慢，需要：
- ✅ 使用较小的网格（50×60×70）
- ✅ 重新校准参数以适应这个网格
- ✅ 找到合理的参数值

---

## 当前设置

### 网格大小（正常模式）
- nx = 50
- nb = 60
- nk = 70
- 总状态点数 = 210,000

### 容差设置（校准模式）
- tol_vfi = 1e-4
- tol_dist = 1e-3
- tol_bhat = 1e-5

### 当前参数值（来自estim_params.txt）
- 这些参数值是为MATLAB的网格（60×80×100）校准的
- 可能不适合Python的网格（50×60×70）

---

## 重新校准方案

### 方案1：完整校准（推荐）

**步骤**：
1. 使用校准模式网格（30×40×50）进行校准
2. 运行完整校准（100次迭代）
3. 找到适合的参数值
4. 使用这些参数值在正常模式网格（50×60×70）上运行

**优点**：
- 校准速度快（使用较小网格）
- 找到适合的参数值
- 结果应该合理

**缺点**：
- 需要较长时间（8-23小时）
- 参数值可能与MATLAB不同

**命令**：
```bash
python run_calibration.py
```

---

### 方案2：快速测试校准

**步骤**：
1. 使用校准模式网格（30×40×50）
2. 运行快速测试（5-10次迭代）
3. 查看参数变化趋势
4. 如果改善，继续完整校准

**优点**：
- 快速验证（30-90分钟）
- 可以提前看到改善

**缺点**：
- 可能不够充分
- 需要后续完整校准

**命令**：
```bash
python quick_test_calibration.py
```

---

### 方案3：调整初始猜测值

**如果校准失败**：
1. 从MATLAB结果中获取更多信息
2. 调整初始猜测值
3. 调整参数边界
4. 重新运行校准

---

## 校准设置建议

### 网格大小（校准模式）
```python
if par.get('do_calib', 0) == 2:
    par['nx'] = 30  # 保持较小以加快校准
    par['nb'] = 40
    par['nk'] = 50
```

### 容差设置（校准模式）
```python
if par.get('do_calib', 0) == 2:
    par['tol_vfi'] = 1e-4  # 平衡精度和速度
    par['tol_dist'] = 1e-3
    par['tol_bhat'] = 1e-5
```

### 优化器设置
```python
options = {
    'maxiter': 100,  # 足够的迭代次数
    'disp': True,
    'ftol': 1e-4,
    'gtol': 1e-4
}
```

---

## 预期结果

### 校准前（使用MATLAB参数）
- output_small: 54.774（857倍于MATLAB）
- LHS: -54.54（负值）
- K_corp: -756.70（负值）

### 校准后（预期）
- output_small: 接近合理值（可能0.05-0.1）
- LHS: 接近正值
- K_corp: 接近正值
- 模型矩接近数据矩

---

## 监控校准过程

### 关键指标
1. **目标函数值**：应该逐渐下降
2. **参数值变化**：应该稳定收敛
3. **市场出清条件**：K_corp和LHS应该变为正值
4. **模型矩匹配**：应该接近数据矩

### 检查点
- 每10次迭代检查一次
- 如果目标函数不再下降，可以停止
- 如果参数值稳定，可以停止

---

## 时间估算

### 快速测试（5次迭代）
- 时间：30-90分钟
- 可以快速验证

### 完整校准（100次迭代）
- 时间：8-23小时
- 建议后台运行

---

## 建议的步骤

### 步骤1：快速测试
```bash
python quick_test_calibration.py
```
- 验证校准过程是否正常
- 查看参数变化趋势
- 预计时间：30-90分钟

### 步骤2：完整校准
```bash
python run_calibration.py
```
- 运行完整校准（100次迭代）
- 找到合理的参数值
- 预计时间：8-23小时

### 步骤3：验证结果
```python
# 设置 do_calib = 0
python main.py
```
- 使用校准后的参数值
- 在正常模式网格（50×60×70）上运行
- 验证结果是否合理

---

## 注意事项

### 1. 参数文件更新
- 校准过程中会更新`inputs/estim_params.txt`
- 建议备份原始文件

### 2. 市场出清惩罚项
- Python添加了市场出清惩罚项
- 这应该能引导优化器找到合理的参数值

### 3. 收敛检查
- 如果目标函数不再下降，可以提前停止
- 如果参数值稳定，可以提前停止

---

## 总结

**策略**：
1. ✅ 使用较小的网格（30×40×50）进行校准
2. ✅ 运行完整校准（100次迭代）
3. ✅ 找到适合的参数值
4. ✅ 使用这些参数值在正常模式网格（50×60×70）上运行

**预期**：
- 校准后应该得到合理的结果
- output_small应该接近合理值
- LHS和K_corp应该变为正值

**时间**：
- 快速测试：30-90分钟
- 完整校准：8-23小时













